<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.NFC" />
    <uses-feature android:name="android.hardware.nfc" android:required="false" />
    <application
        android:label="Have You Fed The Dog?"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <!-- FCM default channel id: matches the channel created in
             notification_service.dart and used by the worker service.
             Background messages get routed here automatically. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="chore_completions" />
        <!-- Small icon for background/system-tray pushes. White paw
             silhouette; Android tints it by the alpha channel. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_stat_notification" />
        <!-- Digital Asset Links: associates the app with
             api.haveyoufedthedog.com so credentials saved on the PocketBase
             password-reset web page autofill in the app. Pairs with the
             assetlinks.json served at that domain (see
             server/pb_public/.well-known/assetlinks.json). -->
        <meta-data
            android:name="asset_statements"
            android:resource="@string/asset_statements" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <!-- Turn OFF Flutter's built-in deep linking so it doesn't feed the
                 https://… App Link URL to GoRouter (which has no route for it,
                 and would show "Page Not Found"). The app_links plugin handles
                 these links separately - see DeepLinkHandler. -->
            <meta-data
              android:name="flutter_deeplinking_enabled"
              android:value="false"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- App Links: tapping a https://haveyoufedthedog.com/join or
                 /claim link opens the app straight to the right screen. The
                 app_links plugin delivers the intent to Dart (DeepLinkHandler);
                 no Kotlin change needed. autoVerify pairs with the
                 handle_all_urls statement in the website's assetlinks.json -
                 which must be served from the haveyoufedthedog.com host and
                 list the Play app-signing key (a sideload won't verify). -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="https" android:host="haveyoufedthedog.com"/>
                <data android:pathPrefix="/join"/>
                <data android:pathPrefix="/claim"/>
                <data android:pathPrefix="/nfc-tap"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
